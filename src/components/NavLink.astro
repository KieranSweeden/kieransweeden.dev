---
export interface Props {
  label: string;
  path: string;
}

const { label, path } = Astro.props;
---

<a
  href={path}
  class=`block nav-link text-zinc-500 hover:text-zinc-50 hover:underline`
  >{label}</a
>
