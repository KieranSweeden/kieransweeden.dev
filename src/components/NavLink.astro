---
export interface Props {
  label: string;
  path: string;
}

const { label, path } = Astro.props;
---

<a
  href={path}
  class=`block text-zinc-500 dark:text-zinc-500 hover:text-zinc-100 hover:underline`
  >{label}</a
>
